nasm -felf64 hello.asm && ld hello.o && ./a.out

dd 32-bit
dw 16-bit
db- 8bit
dq 8b bit

rdi #file handle number
%1 in macro means first parameter

data section is in external memery
code segment also resides in external memery


func assignment:
    shift num right to get lsb
    if data <= 9 , add 30h
    else data > 9 , add 37h

    call display functoin

    bitwise and the num with 0fh
    if data <= 9 , add 30h
    else data > 9 , add 37h

    call display functoin


to avoid repetative coding:
macro
    to display a num
func
    to diplay two nums